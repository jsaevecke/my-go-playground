# --- SECURITY & SECRETS ---
# Never commit credentials, even if they are for local development
.env
.env.*
!.env.example
*.pem
*.key
*.cert
secrets/
ssh_config

# --- GO & BINARIES ---
# Exclude all compiled binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
# Coverage and profiling outputs
*.out
*.prof
# Go workspace and vendor
go.work
go.work.sum
vendor/
# Temporary build artifacts
tmp/

# --- PULUMI & INFRASTRUCTURE ---
# Exclude Pulumi's local state and sensitive metadata
.pulumi/
# Pulumi access tokens or temporary configuration
pulumi.access.token
# Terraform/Tofu leftovers if used alongside
.terraform/
*.tfstate
*.tfstate.backup

# --- KUBERNETES & KIND ---
# Local kubeconfigs and cluster state
kind-kubeconfig
kubeconfig
kube-config
.kube/
kind-config.yaml.local

# --- DOCKER ---
# Local docker build logs or temporary data
.docker/
docker-compose.override.yml

# --- IDE & SYSTEM ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- MONITORING (TOOL SPECIFIC) ---
# Prevent local DB data from being committed
/tools/
/prometheus/data/
/grafana/data/
